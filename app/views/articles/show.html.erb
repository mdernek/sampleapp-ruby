<p>
    <strong>Title:</strong>
    <%= @article.title %>
</p>
<p>
    <strong>Text:</strong>
    <%= simple_format(@article.text) %>
</p>

<h2>Comments</h2>
<%= render 'comments/comment_admin', comments: @article.comments %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>